import 'package:flutter/material.dart';

class ButtonSpacer extends StatelessWidget {
  const ButtonSpacer({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(width: 64.0);
  }
}
